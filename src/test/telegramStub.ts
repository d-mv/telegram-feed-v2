export class TelegramClient {
  start() {
    throw new Error(
      'TelegramClient stub: install telegram@browser to use this in runtime',
    )
  }
}

export class Api {
  static Message = class {}
}
